__pycache__/
*.pyc
.venv/
venv/
.env
# data/articles.json is now fetched directly by the dashboard (static/js/app.js)
# and must be committed/published, so it is intentionally NOT ignored here.
data/*.log
